---
title: Documentation
summary: Everything the API does, versioned with the code that does it.
version: v2
updated: 2026-09-01
---

These pages live in `content/docs` and are reviewed in the same pull request as the code
that made them wrong. There is no CMS behind them and no browser step to publish one.

Start with [webhooks](/docs/v2/webhooks), or read the [quickstart](/docs/v2/quickstart).

:::callout{tone=note title="Where this page comes from"}
This paragraph is a `richTextBlock` and the box around it is a `callout` — the same two
block types the studio offers, parsed out of markdown by the git adapter.
:::
